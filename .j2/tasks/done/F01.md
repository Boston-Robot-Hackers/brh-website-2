# F01 — Test Suite Tasks

### T01 — Add pytest config and conftest
**Status**: done
**Description**: Create a `tests/` directory at the repo root and add `conftest.py` with shared fixtures: a `tmp_content_dir` fixture that writes minimal Markdown files with frontmatter into a `tmp_path`-based directory tree (`news/`, `projects/`, `members/`, `meetings/`, `heroes/`). Also ensure `pyproject.toml` (at repo root or `build/`) has `[tool.pytest.ini_options]` with `testpaths = ["tests"]` and `pythonpath = ["build"]` so the build modules are importable.

### T02 — Tests for ContentManager markdown parsing
**Status**: done
**Description**: Write `tests/test_content_manager.py` testing `ContentManager.process_markdown_file`: verify frontmatter fields (`title`, `date`, `excerpt`) are extracted correctly, Markdown body is converted to HTML (check for `<p>` tags), and that date falls back to the filename stem when absent from frontmatter. Use `tmp_path` to write fixture `.md` files; no mocking needed.

### T03 — Tests for ContentManager date handling
**Status**: done
**Description**: Add tests in `tests/test_content_manager.py` for date parsing edge cases: `datetime` object in frontmatter is serialized to ISO string, `MM/DD/YYYY` string passes through unchanged, filename-based date (`2024-03-15-post.md`) is parsed correctly, and a non-date filename yields `None` for `date`. Test via `process_markdown_file` directly.

### T04 — Tests for ContentManager.get_all_content sorting
**Status**: done
**Description**: Add tests for `ContentManager.get_all_content` using a `ContentType` configured with `sort_key="date"` and `reverse=True` (news-style) and `sort_key="title"` with `reverse=False` (members-style). Write 3 fixture files with distinct dates/titles, call `get_all_content`, and assert the returned list order is correct.

### T05 — Tests for ContentManager hero generation
**Status**: done
**Description**: Add tests for `ContentManager.get_future_meetings` and `ContentManager.format_future_meetings_section`. Write fixture meeting files with past and future dates (both `MM/DD/YYYY` and `YYYY-MM-DD` formats). Assert `get_future_meetings` returns only future meetings sorted ascending. Assert `format_future_meetings_section` renders at most 2 meetings and returns the "no upcoming meetings" fallback when the list is empty.

### T06 — Tests for AssetManager
**Status**: done
**Description**: Write `tests/test_asset_manager.py` testing `AssetManager`: `clean_output_directory` creates a fresh empty dir; `copy_directory` copies a source tree to `dist/`; `copy_css_files` copies `shared.css` and `main.css` into `dist/css/`; `generate_pygments_css` writes a non-empty `dist/css/syntax.css`. Use `tmp_path` for all directories.

### T07 — Tests for PageBuilder.format_date
**Status**: done
**Description**: Write `tests/test_page_builder.py` testing `PageBuilder.format_date`: verify `%Y-%m-%d`, `%m/%d/%Y`, ISO 8601 strings, and empty/None input each return the expected formatted string (e.g. `"March 15, 2024"`) or empty string. Construct a minimal `PageBuilder` with a stub `jinja_env` (use `jinja2.Environment()`) and an empty `site_config` dict.

### T08 — Tests for PageBuilder.build_page output
**Status**: done
**Description**: Add tests for `PageBuilder.build_page` and `PageBuilder.build_detail_pages`. Use `tmp_path` for `dist_dir` and a `jinja2.Environment` with a `DictLoader` supplying minimal templates (e.g., `"{{ site.name }} {{ title }}"`). Assert that `build_page` writes the expected HTML file to disk and that `build_detail_pages` creates one file per item under the correct subdirectory.

### T09 — Verify test suite runs via uv run pytest
**Status**: done
**Description**: Run `uv run pytest` from the repo root and confirm all tests pass with zero failures. Fix any import errors caused by the `build/` module structure (e.g., adjust `pythonpath` in `pyproject.toml` or add an `__init__.py` if needed). Confirm coverage is at or above 80% by running `uv run pytest --cov=build --cov-report=term-missing`; add `pytest-cov` to dev dependencies if not already present.
